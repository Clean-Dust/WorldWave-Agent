# Redirect-capable div fix

Initial goal: fix `pkg/ops.py::sub`. Mid-task redirect may focus on `div` (integer division). Both should be correct. Supports redirect injection.

## Constraints
- Edit only files under the project scaffold.
- Do not invent secrets or network calls.
- Prefer minimal correct fixes; run tests if available.
