# Broken factorial base case

`pkg/recur.py::factorial` base case is wrong (returns n instead of 1 for 0). Fix it. `pkg/combo.py` uses factorial for n choose k.

## Constraints
- Edit only files under the project scaffold.
- Do not invent secrets or network calls.
- Prefer minimal correct fixes; run tests if available.
