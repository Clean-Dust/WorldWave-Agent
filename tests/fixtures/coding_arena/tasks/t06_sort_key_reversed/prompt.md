# Sort key reversed

`pkg/rank.py::top_scores` should return highest scores first but sorts ascending. Fix sort order. `pkg/leaderboard.py` formats the list.

## Constraints
- Edit only files under the project scaffold.
- Do not invent secrets or network calls.
- Prefer minimal correct fixes; run tests if available.
