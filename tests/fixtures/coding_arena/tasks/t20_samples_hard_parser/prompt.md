# Hard CSV parser (samples>=2)

`pkg/csvkit.py::parse_line` must split on commas but respect simple double quotes. Hard task — sample repair path enabled (samples>=2). Multi-file with `pkg/loader.py`.

## Constraints
- Edit only files under the project scaffold.
- Do not invent secrets or network calls.
- Prefer minimal correct fixes; run tests if available.
