# Redirect-capable mul fix

Initial goal: fix `pkg/mathx.py::add`. Mid-task redirect may focus on `mul` instead. Both should be correct for full credit on hidden tests. Supports redirect injection path.

## Constraints
- Edit only files under the project scaffold.
- Do not invent secrets or network calls.
- Prefer minimal correct fixes; run tests if available.
