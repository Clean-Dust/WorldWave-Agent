# Skills

This directory stores AI agent skill definitions — reusable procedural knowledge
that agents load at runtime to perform specific tasks. Each skill is a
SKILL.md file with YAML frontmatter and structured markdown instructions.

Skills are managed by the agent's skill system and can be created, updated,
or deleted at runtime.
